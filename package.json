{
  "name": "SAEON-worldwind-earth-explorer",
  "version": "2.0.0",
  "private": true,
  "homepage": "https://worldwind.earth/explorer",
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "gh-pages": "^1.2.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "gh-pages -d root"
  }
}
